# 과제 제출 (SUBMISSION.md)

## 1. 문제 재정의
대체투자 심사 프로세스에서 가장 큰 병목은 방대한 문서에서 데이터를 단순히 추출하는 것이 아닙니다. "문서마다 수치가 다르고, 환율과 같은 가정이 수시로 변하며, 수많은 이해관계자의 의견(이메일, 회의록)이 흩어져 있는 **데이터의 불확실성과 모순(Conflict)**"을 어떻게 통제할 것인가가 핵심입니다. 
따라서 본 과제는 "단순히 문서를 파싱하여 데이터베이스에 넣는 시스템"이 아니라, **"모순을 발견했을 때 이를 덮어쓰지 않고 이력(Lineage)을 남겨 인간(또는 승인된 AI)에게 안전하게 결정을 위임하는 추적 가능 모델(Traceable Model)을 설계하는 것"**으로 문제를 재정의했습니다.

## 2. 사용자 및 성공 지표
- **주 사용 대상자**: 대체투자 심사역, 리스크 관리 담당자, 데이터 검증자.
- **성공 지표**:
  - **Fail-Safe 달성률**: 시스템이 데이터 충돌을 감지했을 때 임의로 오판하지 않고 `REVIEW_REQUIRED` 상태로 안전하게 격리하는 비율 (100% 목표).
  - **추적성(Traceability) 확보**: 최종 승인된 데이터가 어떤 문서를 근거로 도출되었는지(Lineage) 역추적 가능한 비율.
  - **심사 소요 시간 단축**: AI가 모순된 데이터 간의 문맥을 판단하여 인용구(Quote)를 제시함으로써, 심사역이 수작업으로 문서를 대조하는 시간을 절약.

## 3. 현실적 복잡성 (Real-world Complexity)
현업의 대체투자 딜은 정형화되어 있지 않습니다.
- **포맷의 다양성**: IM(투자설명서), 재무모델(Excel), Term Sheet(PDF), 이메일 등 출처와 포맷이 중구난방입니다.
- **가정의 변동성**: 환율, 금리 등의 가정이 수시로 변동되어 이전에 계산된 LTV나 IRR 값이 언제든 무효화될 수 있습니다.
- **모호한 정성 평가**: 신용도, 환경 리스크 등은 수치화하기 힘든 정성적 텍스트로만 존재합니다.
본 프로젝트는 `SCENARIOS.md`에 명시된 10가지 시나리오(LTV 모순, 금액 단위 오류, 환율 변동 등)를 통해 이러한 현실적 복잡성을 시스템에 주입하고 테스트했습니다.

## 4. 가정 및 대안 (Assumptions & Alternatives)
- **가정**: 
  - 심사역(인간)이 최종 승인을 내릴 때 가장 중요하게 보는 것은 "수치의 출처"와 "변경 사유"입니다. 따라서 모든 핵심 필드 값은 반드시 원문에서 발췌한 `인용구(Quote)`를 동반해야만 신뢰할 수 있는 데이터로 취급합니다.
  - 최신 타임스탬프를 가진 문서(예: 방금 도착한 이메일)의 정보가 구버전 문서보다 높은 우선순위를 가집니다.
- **대안적 접근**: 
  - 단순 Rule-based 병합 전략을 사용할 수도 있었으나, 대체투자의 특성상 1%의 오차가 치명적일 수 있어 `TraceableField<T>` 래퍼 클래스를 도입하여 원본 보존과 이력 추적을 강제하는 대안을 채택했습니다.

## 5. 제외 범위 (Out of Scope)
- **물리적 문서 파일(PDF, Excel) 직접 파싱 및 OCR 모듈**:
  - 방대한 실물 문서를 OCR 처리하는 과정은 과제 본질(불확실성 제어 모델 설계)에서 벗어난다고 판단하여 제외했습니다. 대신, 문서 스크래핑이 이미 완료되었다고 가정한 비정형 텍스트를 `DataGenerator`가 뱉어내는 방식으로 대체하였습니다.
- **실제 외부 AI API(Gemini) 통신 연동**: 
  - 평가의 편의성과 외부 유료 API 종속성 배제 원칙에 따라, 실제 HTTP 통신 대신 `GeminiAIService` 클래스 내부에서 AI의 행위(문맥 판단 및 인용구 반환)를 Mocking하여 시뮬레이션하도록 대체 구현하였습니다.

## 6. AI 활용 전략 및 회고
- **AI 활용 전략 (환각 통제)**: 
  - 단순 텍스트 요약이 아닌 **"충돌 해소(Conflict Resolution)"**의 중재자로 AI를 활용했습니다.
  - LLM의 가장 큰 약점인 환각(Hallucination) 현상을 방지하기 위해, AI가 값을 추론할 때 반드시 원문의 **인용구(Quote)**를 추출하도록 구조화하여 파이프라인을 설계했습니다. 
- **회고**:
  - 데이터 엔지니어링 관점에서 "불확실성을 코드 레벨(`TraceableField`)에서 어떻게 캡슐화할 것인가"에 대한 깊은 고민을 할 수 있었습니다.
  - AI를 단순히 '마법 지팡이'처럼 모든 것을 해결하는 도구로 방치하지 않고, 룰 엔진(Rule Engine)과 결합하여 '안전한 보조 도구'로 통제하는 파이프라인(AI-in-the-loop)을 구축한 것이 이번 프로젝트의 가장 큰 성과라고 생각합니다.
